import {useEffect,useRef} from "react";import maplibregl from "maplibre-gl";export default function OperationalMap(){const ref=useRef(null);useEffect(()=>{if(!ref.current)return;const map=new maplibregl.Map({container:ref.current,style:"https://demotiles.maplibre.org/style.json",center:[-72.7,-36.8],zoom:5});map.addControl(new maplibregl.NavigationControl());return()=>map.remove()},[]);return <div className="map-panel"><h3>Mapa operacional · MapLibre</h3><div ref={ref} className="maplibre-box"/><small className="note">Ubicación individual de recursos queda como feature futura.</small></div>}